#!/usr/bin/python3
def square_matrix_simple(matrix=[]):
    result_matrix = []

    for row in matrix:
        result_row = []

        for a in row:
            new_row.append(x ** 2)

        result_matrix.append(new_row)

    return result_matrix
